export const CONFIG = {
  player: {
    modelUrl: '/assets/models/player.glb',
    runSpeed: 4.2,          // м/с, постоянная: стик задаёт направление, а не темп
    radius: 0.38,           // радиус персонажа для столкновений с препятствиями
    turnSpeed: 12,          // рад/с доворот корпуса к направлению движения
    runClipSpeed: 4.2,      // скорость, на которую рассчитан клип Run (нет проскальзывания стоп)
  },
  props: {
    libraryUrl: '/assets/models/props.glb',   // библиотека объектов из Env.blend
  },

  locations: {
    first: 'farm',   // какая локация грузится без ?location= в адресе
    // Сквозь что можно проходить: мелочь под ногами, аптечки, агрополя и дорожное покрытие.
    // Перекати-поле названо поимённо: из scatter оно выходит раздутым до 2 м и иначе
    // попадало бы в препятствия, хотя это пучок сухой травы.
    passablePrefixes: ['Prop_', 'Food_', 'Grass_', 'Crop_', 'Road_', 'Bush_Tumbleweed'],
    // …и всё, что ниже этой высоты: мелкий кустарник, перекати-поле, обломки.
    // Живая изгородь и можжевельник выше порога и остаются препятствием.
    passableHeight: 1.5,
    // Забор держит периметр локации, поэтому под правило высоты не попадает:
    // жерди ранчо всего 1.35 м, но проходить сквозь них нельзя.
    alwaysBlocking: ['Fence_'],
  },

  joystick: {
    size: 150,        // диаметр базы, px
    knobSize: 66,     // диаметр ручки, px
    overhang: 10,     // насколько ручке позволено вылезать за базу
    side: 'right',    // с какой стороны экрана держать стик: 'right' | 'left'
    margin: 34,       // отступ от нижнего угла
    deadZone: 0.12,   // ниже этого отклонения ввод считается нулевым
    floating: false,  // true — база прыгает под палец при касании
  },

  world: {
    skyColor: 0x9db4c8,      // пасмурное небо
    groundColor: 0x55595e,
    sunColor: 0xfff4e2,
    sunOffset: [16, 22, -8],  // откуда светит солнце; при изометрии тени должны уходить вбок, а не за персонажа
    sunIntensity: 2.6,
    hemiIntensity: 1.6,
    envIntensity: 0.9,       // сила отражённого света от окружения
    fogNear: 60,
    fogFar: 180,
    exposure: 1.0,
  },

  camera: {
    viewSize: 18,        // сторона квадрата в метрах, который гарантированно виден при любом соотношении сторон
    yaw: 45,             // градусы, классический разворот изометрии
    pitch: 30,           // градусы над горизонтом; 26.565 — «2:1» диметрия старых игр
    distance: 45,        // относ камеры от цели; на орто-проекцию масштаб не влияет
    lookAtHeight: 1.0,   // на какую высоту персонажа целимся
    damping: 12,         // скорость, с которой камера догоняет цель
  },
};
